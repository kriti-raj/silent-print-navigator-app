// API Interceptor for handling mock data and future API calls
export const initializeApiInterceptor = () => {
  // This can be used for intercepting API calls
  // Currently just initializing the system
  console.log('API Interceptor initialized');
};